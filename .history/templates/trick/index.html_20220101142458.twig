{% extends 'base.html.twig' %}
{% import 'macros/alertes.html.twig' as utils %}

{% block title %}Figures | ACCUEIL{% endblock %}

{% block main %}
    <main id="main">
        {% for trick in paginator.results %}
            <div class="row">
                <div class="card mb-3 col-md-2 mx-auto">
                    <a href="#" title=""><img src="{{ path('trick_index') }}/trick.trickThumbnail" class="card-img-top" alt="..."></a>
                    <div class="card-body">
                        <h5 class="card-title"><a href="" title="">Card title</a></h5>
                        <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
                    </div>
                </div>
            </div>
        {% else %}

        {% endfor %}
    </main>
{% endblock %}
